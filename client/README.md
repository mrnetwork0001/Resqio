# Resqio Dashboard (Next.js 14)

Planned for Week 3 (Aug 31 – Sept 7): a read-only community situation board —
active crisis assessment, the offer/request board, match lifecycle, and the
ping log — backed by the pipeline's `status()` endpoint
(`{"action": "status"}` on AgentCore, or the local webhook server).

Scaffold (to be generated):

```bash
npx create-next-app@latest . --ts --tailwind --app
```
